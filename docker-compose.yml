version: '3.8'
services:
  telegram-downloader:
    build: .
    container_name: telegram-downloader
    env_file:
      - .env
    volumes:
      - ./downloads:/app/downloads
      - ./session_data:/app/session_data
    command: ["--all"]
    restart: "no"
